<?xml version="1.0" encoding="utf-8"?>
<!--
  Root layout with fitsSystemWindows="true".
  Android automatically applies system-bar insets as padding on this view,
  so all children stay within the safe area — no manual insets code needed.
  Requires: WindowCompat.setDecorFitsSystemWindows(window, false) in Java.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0d1117"
    android:fitsSystemWindows="true">

    <!-- TopOnlySwipeRefreshLayout: only triggers refresh on pull from top 200dp zone.
         Prevents accidental refresh when scrolling mid-page content. -->
    <com.fileservice.app.TopOnlySwipeRefreshLayout
        android:id="@+id/swipe_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="48dp">

        <WebView
            android:id="@+id/web_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </com.fileservice.app.TopOnlySwipeRefreshLayout>

    <!-- Page-loading progress bar. Gravity.TOP: sits just below status bar (fitsSystemWindows handles this). -->
    <ProgressBar
        android:id="@+id/progress_bar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_gravity="top"
        android:max="100"
        android:progressTint="#00d4ff"
        android:progressBackgroundTint="@android:color/transparent"
        android:visibility="gone" />

    <!-- Splash screen overlay. Hidden with fade-out on first page load. -->
    <FrameLayout
        android:id="@+id/splash_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#0d1117"
        android:clickable="true"
        android:contentDescription="启动画面">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:orientation="vertical">

            <ImageView
                android:layout_width="96dp"
                android:layout_height="96dp"
                android:src="@mipmap/ic_launcher"
                android:contentDescription="应用图标" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:paddingTop="16dp"
                android:text="FileService"
                android:textColor="#e0e6f0"
                android:textSize="22sp"
                android:textStyle="bold" />

            <ProgressBar
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="center"
                android:layout_marginTop="28dp"
                android:indeterminate="true"
                android:indeterminateTint="#2196F3" />
        </LinearLayout>
    </FrameLayout>

    <!-- Bottom toolbar. Gravity.BOTTOM: sits just above nav bar (fitsSystemWindows handles this). -->
    <LinearLayout
        android:id="@+id/bottom_bar"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_gravity="bottom"
        android:background="#161b22"
        android:elevation="8dp"
        android:gravity="center"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btn_home"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="⌂"
            android:textAllCaps="false"
            android:textColor="#8b949e"
            android:textSize="18sp" />

        <Button
            android:id="@+id/btn_refresh"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="⟳"
            android:textAllCaps="false"
            android:textColor="#8b949e"
            android:textSize="18sp" />

        <Button
            android:id="@+id/btn_back"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="◀"
            android:textAllCaps="false"
            android:textColor="#8b949e"
            android:textSize="18sp" />

        <Button
            android:id="@+id/btn_settings"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="⚙"
            android:textAllCaps="false"
            android:textColor="#8b949e"
            android:textSize="18sp" />
    </LinearLayout>

</FrameLayout>
